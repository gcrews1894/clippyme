name: CI

# Runs the host-runnable (non-integration) test suite, a dependency
# vulnerability scan, and the frontend build on every push and PR.
# Heavy CV/ML integration tests (cv2/mediapipe/scenedetect) run in Docker
# and are intentionally excluded here (`-m "not integration"`).

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  workflow_dispatch: {}

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend (pytest + pip-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install package (editable) + host-test deps + audit tooling
        run: |
          python -m pip install --upgrade pip
          # `.[host-tests]` pulls the light runtime subset the non-integration
          # suite imports (numpy/pydantic/fastapi/yt-dlp/…). `pip install -e .`
          # alone installs no runtime deps; the heavy CV/ML stack stays in Docker.
          pip install -e ".[host-tests]"
          pip install pytest pip-audit ruff

      # Lint for real bugs only — undefined names (F82, the class behind the
      # silently-swallowed MAR NameError regression), syntax errors (E9),
      # logic mistakes (F7) and assert/comparison bugs (F63). Style/unused-import
      # noise (F401/F841) is intentionally NOT gated to keep this green + signal.
      - name: Lint (ruff — bug-class rules)
        run: ruff check src/clippyme tests --select E9,F63,F7,F82

      - name: Run host test suite (excludes integration)
        run: pytest -m "not integration" -q

      # Audit the pinned tree for known CVEs. Now BLOCKING (we're on a lock
      # file): a newly-disclosed CVE in any pinned dep fails CI so it gets
      # reviewed before merge. Ignored IDs, each with rationale:
      #   - CVE-2025-3000 (torch): fixed in 2.7.0; we pin 2.11.0 > fix, so the
      #     pip-audit hit is a stale/over-broad OSV range (false positive).
      #   - CVE-2026-24049 (wheel): build-time-only ReDoS in metadata parsing,
      #     not reachable at runtime.
      #   - PYSEC-2026-1805 (protobuf 4.25.9): DoS via deeply-nested Any in
      #     json_format.ParseDict(). Unreachable: ClippyMe never parses
      #     attacker-controlled protobuf JSON — protobuf is a mediapipe
      #     transitive used only for its bundled model/graph configs. The fix
      #     versions (5.29.6/6.33.5) violate mediapipe 0.10.14's protobuf<5
      #     constraint; drop this ignore when the documented mediapipe/torch
      #     upgrade procedure (requirements.txt header) is executed.
      - name: Dependency vulnerability scan (full pinned tree)
        run: >-
          pip-audit -r requirements.lock --desc
          --ignore-vuln CVE-2025-3000
          --ignore-vuln CVE-2026-24049
          --ignore-vuln PYSEC-2026-1805

  frontend:
    name: Frontend (test + build + lint)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dashboard
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      # Vitest (jsdom): the pure-function suites + the component tests for the
      # edit/compose flows. These were previously never executed in CI.
      - name: Test
        run: npm test

      - name: Build
        run: npm run build

  # Heavy CV/ML integration suite (cv2 + mediapipe + scenedetect + the reframe
  # render path). Too slow/large for every PR, so it's gated to main pushes and
  # manual runs. Mirrors the documented local command in CLAUDE.md.
  integration:
    name: Integration (Docker — reframe/pipeline)
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - name: Run integration suite in the backend image
        run: docker compose run --rm -u root backend sh -lc "pip install -q pytest && pytest -m integration -q"
